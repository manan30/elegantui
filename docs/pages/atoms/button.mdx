---
title: Button
---

import { Button } from '@elegantui/atoms';

# Button

Buttons allow users to perform actions and choose with a single tap.

## Usage

```jsx
import { Button } from '@elegantui/atoms';
```

## Basic Example

<Button style={{ marginTop: '1rem' }}>Hello Friend</Button>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return <Button>Hello Friend</Button>;
}
```

### Variants

Button component is available in six variants.

<div
  style={{
    marginTop: '1rem',
    display: 'flex',
    alignItems: 'center',
    gap: '0.5rem',
    overflowX: 'auto'
  }}
>
  <Button variant='primary'>Primary</Button>
  <Button variant='secondary'>Secondary</Button>
  <Button variant='error'>Error</Button>
  <Button variant='warning'>Warning</Button>
  <Button variant='success'>Success</Button>
  <Button>Default</Button>
</div>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return (
    <Button
      variant='primary' // 'default' | 'secondary' | 'success' | 'warning' | 'error'
    >
      Hello Friend
    </Button>
  );
}
```

### Sizes

Button component is available in three sizes.

<div
  style={{
    marginTop: '1rem',
    display: 'flex',
    alignItems: 'center',
    gap: '0.5rem',
    overflowX: 'auto'
  }}
>
  <Button size='sm' variant='primary'>
    Small
  </Button>
  <Button size='md' variant='primary'>
    Medium
  </Button>
  <Button size='lg' variant='primary'>
    Large
  </Button>
</div>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return (
    <Button
      size='sm' // 'md' | 'lg'
      variant='primary'
    >
      Hello Friend
    </Button>
  );
}
```

### Appearance

Button component is available in four appearances.

<div
  style={{
    marginTop: '1rem',
    display: 'flex',
    alignItems: 'center',
    gap: '0.5rem',
    overflowX: 'auto'
  }}
>
  <Button variant='primary' appearance='solid'>
    Solid
  </Button>
  <Button variant='primary' appearance='outline'>
    Outline
  </Button>
  <Button variant='primary' appearance='link'>
    Link
  </Button>
  <Button variant='primary' appearance='ghost'>
    Ghost
  </Button>
</div>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return (
    <>
      <Button
        variant='primary'
        appearance='solid' // 'outline' | 'link' | 'ghost'
      >
        Solid
      </Button>
    </>
  );
}
```

### States

Button component can be configured to have `loading` and `disabled` states by passing props of the same name respectively.

<div
  style={{
    marginTop: '1rem',
    display: 'flex',
    alignItems: 'center',
    gap: '0.5rem',
    overflowX: 'auto'
  }}
>
  <Button variant='primary' style={{ marginTop: '1rem' }} loading>
    Loading
  </Button>
  <Button variant='primary' style={{ marginTop: '1rem' }} disabled>
    Disabled
  </Button>
</div>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return (
    <>
      <Button variant='primary' style={{ marginTop: '1rem' }} loading>
        Loading
      </Button>
      <Button variant='primary' style={{ marginTop: '1rem' }} disabled>
        Disabled
      </Button>
    </>
  );
}
```

### Custom styles

Button component allows you to configure your own styles. Styles can be provided either through the React `style` prop or CSS classes using `className` prop.

<Button
  style={{ marginTop: '1rem', backgroundColor: 'chocolate', color: 'white' }}
>
  Custom Styles
</Button>

```jsx
import { Button } from '@elegantui/atoms';

export default function App() {
  return (
    <Button
      style={{
        marginTop: '1rem',
        backgroundColor: 'chocolate',
        color: 'white'
      }}
    >
      Custom Styles
    </Button>
  );
}
```

#### Available Props

---

| **Prop**       | **Values**                                                                                      | **Default** |
| -------------- | ----------------------------------------------------------------------------------------------- | ----------- |
| **variant**    | <code>default &#124; primary &#124; secondary &#124; error &#124; success &#124; warning</code> | `default`   |
| **size**       | <code>sm &#124; md &#124; lg </code>                                                            | `md`        |
| **appearance** | <code>solid &#124; outline &#124; link &#124; ghost </code>                                     | `solid`     |
| **disabled**   | <code>true &#124; false</code>                                                                  | `false`     |
| **loading**    | <code>true &#124; false</code>                                                                  | `false`     |
| **...rest**    | `React.ButtonHTMLAttributes<HTMLButtonElement>`                                                 | --          |
